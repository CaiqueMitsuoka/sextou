<p id="notice"><%= notice %></p>
<h1 class="title">Parties</h1>

<% if @parties.any? %>
  <%= link_to 'Nova Party', new_party_path, class: 'button' %>

  <ul class="list">
    <% @parties.each do |party| %>
      <li class="list__item">
        <h2 class="list__item-title"><%= party.name %></h2>

        <a onClick="copyToClipboard('<%= party_tracks_url(slug: party.slug) %>')" class="button button--listing">Copiar link</a>

        <a href="<%= party_tracks_path(slug: party.slug) %>" class="button button--listing">Vamooo 🎉</a>
      </li>
    <% end %>
  </ul>
<% else %>
  <%= link_to 'Crie sua primeira pa-pa-pa-party 🎉', new_party_path, class: 'subheading' %>
<% end %>
